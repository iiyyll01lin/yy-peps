# Reproducible top-level constraints for Python 3.12.
#
# These versions match the reference environment recorded on 2026-07-22.
# This is intentionally a cross-platform constraints approach, not a frozen
# platform-specific transitive lock. Record the resolved environment for every
# result-producing run with:
#   python -m pip freeze --all > run-environment.txt

numpy==2.4.4
scipy==1.18.0
Pillow==12.2.0
imageio==2.37.4
matplotlib==3.11.1
tqdm==4.67.1
scikit-image==0.26.0
torchmetrics==1.9.0
lpips==0.1.4
flip-evaluator==1.7
trimesh==4.12.2
mesh-to-sdf==0.0.15
jupyter==1.1.1
ipykernel==7.3.0
pytest==9.1.1
