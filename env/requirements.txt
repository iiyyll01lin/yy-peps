# Install with the checked-in constraints:
#   python -m pip install -r env/requirements.txt -c env/constraints.txt
#
# torch/torchvision are selected separately because CPU and ROCm use different
# wheel indexes. See README.md and env/rocm_setup.md.

# Core numerics
numpy
scipy
pillow
imageio
matplotlib
tqdm

# Metrics
scikit-image
torchmetrics
lpips
flip-evaluator
# LSD/LPSD are implemented in peps/metrics.py.

# SDF preprocessing (App 3)
trimesh
mesh-to-sdf

# Notebooks
jupyter
ipykernel

# Dev
pytest
