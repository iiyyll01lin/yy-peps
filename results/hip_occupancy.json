{
  "answer": "Occupancy is 12.5%. The launch asks for 32 KB of LDS per 64-thread workgroup, and each compute unit has 64 KB, so only two workgroups and four waves are resident per CU out of a possible thirty-two. Wave slots would allow sixteen workgroups and the register budget would allow nine, so LDS is roughly four times more restrictive than the next constraint. With four waves per CU there is very little to switch to while a memory access is outstanding, which is consistent with a kernel whose speed tracks compute-unit count and nothing else.",
  "generated_at_utc": "2026-08-07T16:03:04.716696+00:00",
  "gpu_time_share": {
    "average_ns": 12587239,
    "calls": 25,
    "integrated_peps_wmma_percent": 99.99,
    "note": "The kernel accounts for essentially all GPU time and its average matches the HIP-event median of 12.60 ms, so the host-side measurement boundary is not hiding overhead."
  },
  "launch": {
    "kernel": "integrated_peps_wmma",
    "lds_bytes_per_workgroup": 32768,
    "scratch_size": 0,
    "sgpr_count": 128,
    "vgpr_count": 56,
    "waves_per_workgroup": 2,
    "workgroup_threads": 64,
    "workgroups_launched": 65536
  },
  "limitations": [
    "SUPERSEDED: the occupancy figures here were computed from launch geometry and a 64 KB per-CU pool. rocprofv3 OccupancyPercent has since measured them, and confirmed 12.5% for this footprint. See measured_confirmation below.",
    "One kernel, one method, one part; the RDNA4 host has no profiler installed so the same measurement has not been repeated there.",
    "Low occupancy is consistent with the observed scaling but is not proof of the cause; a counter-based bandwidth or stall analysis would be needed to close that. The intervention in results/hip_lds_ab.json is stronger evidence than this correlation."
  ],
  "limits": {
    "limiter": "LDS",
    "units_note": "These counts are per compute unit, which is the vocabulary of the superseded model. They remain arithmetically consistent with the corrected one on this footprint because 32768 bytes gives an even four workgroups per WGP, but a per-CU division cannot express an odd count and is not how allocation behaves. See model_correction.",
    "workgroups_per_cu_by_lds": 2,
    "workgroups_per_cu_by_vgprs": 9,
    "workgroups_per_cu_by_wave_slots": 16
  },
  "measured_confirmation": {
    "counter_max_percent": 12.624,
    "counter_mean_percent": 12.487,
    "counter_min_percent": 12.46,
    "data": "results/hip_profile/gfx1151_occupancy.csv",
    "derived_percent": 12.5,
    "dispatches": 15,
    "tool": "rocprofv3 --pmc OccupancyPercent MeanOccupancyPerCU",
    "verdict": "The derived figure for this footprint was right, but the model behind it was not. See model_correction."
  },
  "model_correction": {
    "corrected_twice": "This model has been wrong twice and both times the counter caught it. The second correction is recorded in results/hip_specialised_caps.json, including a cap chosen specifically to separate the granular model from the two it replaced.",
    "guarded_by": "tests/test_hip_lds_caps.py::test_only_the_granular_model_matches_every_measurement",
    "now": "effective = ceil(footprint / 1024) * 1024, then workgroups per WGP = floor(131072 / effective), which equals waves per CU because a workgroup is two waves and a WGP is two compute units.",
    "status": "The corrected model matches all seven measured footprints within 0.2 percentage points. It is recorded as the model that fits the counter, not as a claim about hardware internals read from a specification.",
    "was": "workgroups per CU = floor(65536 / footprint), from the 64 KB per CU the agent advertises.",
    "what_exposed_it": "13312 bytes gives nine workgroups per WGP, an odd number the per-CU model cannot express: it predicted 25% and the counter measured 28.04%. Three further footprints from per-method specialised builds then measured exactly one workgroup below the per-WGP model, which is what the 1024-byte granule accounts for.",
    "why_it_went_unnoticed": "The models agree on many footprints. Over the seven now measured, the original per-CU model matches five and an intermediate per-WGP model without the granule matches three. Both footprints measured at the time this receipt was written, 32768 and 12288, are among the agreements, so the wrong model produced two correct answers."
  },
  "next_experiments": [
    "DONE, see results/hip_lds_ab.json: the footprint was cut from 32768 to 12288 bytes rather than halved, giving 31.25% occupancy instead of the 25% predicted here, and latency improved 1.70x to 2.13x on both parts with identical checksums. Note the return was sublinear: 2.5x occupancy bought about 2x.",
    "Raise the workgroup from 64 threads so each LDS allocation serves more waves, which changes occupancy without changing the footprint. Not yet tried, and it may compound or conflict with the footprint change above.",
    "PARTLY ANSWERED, see results/hip_lds_ab.json: the Pink path is not intrinsically slower. Both Pink and concat were forced to reserve the same worst-case LDS, so Pink's smaller aggregate (44 against 112) bought nothing. With the narrowed cap the paper's ordering is restored on gfx1151 and the gfx1201 inversion shrinks from 2.28 ms to 0.16 ms."
  ],
  "occupancy": {
    "if_lds_halved": {
      "lds_bytes": 16384,
      "occupancy_fraction": 0.25,
      "superseded_note": "A prediction made with the per-CU model, and superseded twice over. The footprint was cut to 12288 rather than halved, so this case was never run; and the field previously read workgroups_per_cu 4, which is the wrong unit. Under the corrected model 16384 bytes gives eight workgroups per WGP and eight waves per CU, so the 0.25 happens to survive the correction while the workgroup count does not.",
      "workgroups_per_wgp": 8
    },
    "max_waves_per_cu": 32,
    "occupancy_fraction": 0.125,
    "resident_waves_per_cu": 4
  },
  "paper_exact": false,
  "part": {
    "compute_units": 40,
    "generation": "RDNA3.5",
    "isa": "gfx1151",
    "lds_kb_per_cu": 64,
    "max_waves_per_cu": 32,
    "max_waves_per_simd": 16,
    "simds_per_cu": 2,
    "wavefront_size": 32
  },
  "profiler": {
    "host": "aup-HP-Z2-Mini-G1a",
    "mode": "--stats --kernel-trace --output-format csv",
    "note": "Profiled on the RDNA3.5 part because it is the machine with the ROCm profiler suite installed; the RDNA4 host has roc-obj tooling only.",
    "rocm": "7.2.3",
    "tool": "rocprofv3"
  },
  "question": "Why is the fused kernel 1.7x to 4.3x slower than the paper's runtime numbers, and why does RDNA4 buy nothing beyond its compute units?",
  "schema": "peps.hip_occupancy",
  "schema_version": 1,
  "verification_status": "bounded_local_profile_not_paper_comparable"
}
